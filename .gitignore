# Upstream stays as its own repo (not committed into distro)
upstream/

# Keep reverse-engineering artifacts out of the distro repo
research/

# Local noise
**/.DS_Store
limbo.code-workspace
.vscode/

# Deploy outputs
dist/

# Plugin local backup
limbo-plugin/Submodules/Lidarr.bak/

# Plugin build output
limbo-plugin/plugin/bin/
limbo-plugin/plugin/obj/
limbo-plugin/dist/
limbo-plugin/plugin/PluginInfo.cs

# never commit upstream edits; Dockerfile is tracked upstream
!upstream/lidarr-metadata/Dockerfile

# allow upstream submodule
!upstream/
!upstream/lidarr-metadata/
